use std::sync::Arc;
use plexus_core::plexus::DynamicHub;
use crate::activations::*;

pub async fn build_rpc() -> Arc<DynamicHub> {
    // Register all activations under the "{{NAMESPACE}}" hub
    Arc::new(
        DynamicHub::new("{{NAMESPACE}}")
            // .register(YourPlugin::new())
    )
}

use schemars::JsonSchema;
use serde::{Deserialize, Serialize};

/// Events from {{PLUGIN_SNAKE}} operations
#[derive(Debug, Clone, Serialize, Deserialize, JsonSchema)]
#[serde(tag = "type", rename_all = "snake_case")]
pub enum {{PLUGIN}}Event {
    /// Successful result
    Ok {
        message: String,
    },
    /// Error
    Error {
        message: String,
    },
}
